spring:
  sql:
    init:
      mode: always
  config:
    activate:
      on-profile: 'ci,test'
  datasource:
    driverClassName: org.h2.Driver
    url: 'jdbc:h2:mem:shareit'
    username: test
    password: test
